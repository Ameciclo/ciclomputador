export default interface iCrossesData {
  id: string;
  name: string;
  type: string;
  code: number;
  scode: number;
  seg_length: string;
  full_id: string;
  osm_id: number;
  osm_type: string;
  highway: string;
}